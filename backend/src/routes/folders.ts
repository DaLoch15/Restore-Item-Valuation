import { Router } from 'express';

// This file is kept for backwards compatibility but photo routes
// are now handled by folderPhotos.ts
// The main folder CRUD routes are under /api/projects/:projectId/folders

const router = Router();

export default router;
